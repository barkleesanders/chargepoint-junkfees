{
  "name": "chargepoint-junkfees",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev dist",
    "deploy": "vite build && echo '{\"version\":1,\"include\":[\"/api/*\"],\"exclude\":[]}' > dist/_routes.json && wrangler pages deploy dist --project-name chargepoint-junkfees"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@hono/vite-cloudflare-pages": "^0.4.3",
    "@hono/vite-dev-server": "^0.25.0",
    "@cloudflare/workers-types": "^4.20250214.0",
    "vite": "^6.0.0",
    "wrangler": "^4.0.0"
  }
}
